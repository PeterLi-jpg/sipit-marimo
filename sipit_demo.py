# /// script
# requires-python = ">=3.11"
# dependencies = [
#   "marimo>=0.23.0",
#   "torch>=2.3",
#   "transformers>=4.50.0",
#   "numpy>=1.26",
#   "matplotlib>=3.8",
#   "scikit-learn>=1.4",
# ]
# ///
"""Language Models Are Injective — and Hence Invertible.

A marimo notebook walking through Nikolaou et al. (ICLR 2026, arXiv:2510.15511)
for the alphaXiv × marimo competition.

Pre-computed sections (§ 1 base PCA, § 2 loss landscape, § 3 recovery table)
load instantly without the model. Live sections (custom sentences, live search)
require GPT-2 and activate once the model cell finishes loading.
"""

import marimo

__generated_with = "0.23.3"
app = marimo.App(width="medium", app_title="LMs Are Injective")


# ── Imports ───────────────────────────────────────────────────────────────────

@app.cell(hide_code=True)
def _imports():
    import marimo as mo
    import numpy as np
    import matplotlib.pyplot as plt
    from types import SimpleNamespace

    # ── Inlined sipit_toolkit (kept inline so molab/WASM single-file load works) ─

    # ── Color palette ─────────────────────────────────────────────────────────────
    _TRUE_COLOR  = "#10b981"   # true token / success  (green)
    _DIST_COLOR  = "#3b82f6"   # distractor / secondary (blue)
    _WRONG_COLOR = "#ef4444"   # wrong token / failure  (red)
    _PCA_COLORS  = [
        "#3b82f6", "#10b981", "#f59e0b", "#8b5cf6",
        "#0891b2", "#ef4444", "#a16207", "#6366f1",
    ]

    # ── Matplotlib rcParams ───────────────────────────────────────────────────────
    _RCPARAMS = {
        "figure.facecolor":   "white",
        "axes.facecolor":     "white",
        "axes.edgecolor":     "#555555",
        "axes.labelcolor":    "#222222",
        "axes.titlesize":     11,
        "axes.titleweight":   "normal",
        "axes.spines.top":    False,
        "axes.spines.right":  False,
        "xtick.color":        "#555555",
        "ytick.color":        "#555555",
        "grid.color":         "#E5E5E5",
        "grid.linewidth":     0.6,
        "font.family":        "sans-serif",
        "font.size":          10,
        "mathtext.fontset":   "cm",
    }

    # ── Base sentences ────────────────────────────────────────────────────────────
    _BASE_SENTENCES = [
        "The cat sat on the mat",
        "A cat sat on a mat",
        "The dog ran through the park",
        "Machine learning is transforming science",
        "Paris is the capital of France",
        "To be or not to be",
        "The quick brown fox jumps",
        "Statistical inference and probability",
    ]

    # ── Pre-computed PCA (GPT-2 layer 12, mean-pooled, ln_f=Identity) ─────────────
    # _XY_BASE: 2-D projections of the 8 _BASE_SENTENCES hidden states.
    # _PCA_COMPONENTS / _PCA_MEAN: stored for projecting new vectors without sklearn.

    # fmt: off
    _XY_BASE = np.array([
        [-36.12848, -47.08138],
        [-29.98337, -49.85043],
        [ 12.43895, -26.43545],
        [ 38.38483,  21.49386],
        [ 12.68150,   9.84942],
        [-82.32365,  71.58634],
        [ 15.14749, -11.90223],
        [ 69.78265,  32.33986],
    ])

    _PCA_EXPLAINED_VARIANCE_RATIO = [0.3353, 0.2593]
    _MIN_PAIRWISE_DIST = 36.808

    _PCA_COMPONENTS = np.array([
         0.532527,  -0.562131,   0.837946,   0.522211,  -0.405743,  -1.251539,  -0.463887,   0.936043,   1.662440,  -0.014089,
        -2.208738,  -2.471076,  -0.603171,  -1.174895,   0.023686,   2.925431,   0.095067,  -0.203992,   2.028486,  -0.720907,
        -1.262248,  -2.406695,  -0.029160,  -1.433611,   2.397497,  -1.053330,   1.610164,   0.504799,  -2.251441,   1.925872,
         0.531711,   0.066093,  -2.302391,  -1.549082,  -1.562634,  -1.098249,   1.124358,   0.765019,  -1.327014,  -0.762942,
        -0.401247,  -0.497213,   1.705244,  -4.453743,  -0.776048,  -5.413989,  -1.556132,   1.147891,  -2.384098,   0.275917,
        -1.937815,   1.371879,   0.165439,   1.063039,  -1.902419,   0.231489,  -9.705507,  -1.053755,  -0.083916,  -1.848908,
        -0.761484,   0.807924,   9.516601,   0.139224,   2.048263,   0.809441,   0.816381,  -0.539058,
    ])
    # fmt: on


    # ── Pre-computed loss landscape ───────────────────────────────────────────────
    # Prompt: "The cat sat on the mat", layer 12, 256 distractors, seed 42.
    _LANDSCAPE_PROMPT  = "The cat sat on the mat"
    _LANDSCAPE_LAYER   = 12
    _LANDSCAPE_RESULTS = [
        {
            "tok_idx": 0, "true_token": "The",
            "true_loss": 0.0, "rank": 1,
            "min_rand": 1.490, "median_rand": 3.620, "sample_count": 256,
            "top30_losses":   [0.0,    1.490, 2.074, 2.109, 2.112, 2.154, 2.163, 2.165, 2.196, 2.288,
                               2.295,  2.309, 2.340, 2.345, 2.402, 2.422, 2.455, 2.462, 2.469, 2.506,
                               2.512,  2.515, 2.547, 2.549, 2.554, 2.563, 2.602, 2.640, 2.656, 2.660],
            "top30_is_true":  [1] + [0]*29,
        },
        {
            "tok_idx": 1, "true_token": " cat",
            "true_loss": 4.32e-11, "rank": 1,
            "min_rand": 18.25, "median_rand": 31.99, "sample_count": 256,
            "top30_losses":   [4.32e-11, 18.25, 20.24, 21.27, 21.37, 21.69, 21.82, 22.46, 22.56, 22.57,
                               23.16,    23.31, 23.48, 23.58, 23.60, 23.61, 23.66, 23.66, 24.27, 24.69,
                               24.69,    24.72, 24.85, 24.85, 24.85, 24.89, 24.91, 24.99, 25.10, 25.17],
            "top30_is_true":  [1] + [0]*29,
        },
        {
            "tok_idx": 2, "true_token": " sat",
            "true_loss": 5.44e-11, "rank": 1,
            "min_rand": 56.48, "median_rand": 81.56, "sample_count": 256,
            "top30_losses":   [5.44e-11, 56.48, 56.80, 57.22, 58.29, 59.64, 59.75, 62.90, 64.39, 64.44,
                               64.49,    64.94, 65.05, 65.30, 65.68, 65.73, 66.65, 66.73, 66.94, 67.27,
                               67.27,    67.40, 67.52, 67.58, 67.78, 67.80, 68.24, 68.65, 68.71, 68.79],
            "top30_is_true":  [1] + [0]*29,
        },
        {
            "tok_idx": 3, "true_token": " on",
            "true_loss": 4.67e-11, "rank": 1,
            "min_rand": 31.05, "median_rand": 58.74, "sample_count": 256,
            "top30_losses":   [4.67e-11, 31.05, 32.01, 37.37, 40.07, 41.49, 41.56, 41.96, 42.60, 42.62,
                               42.72,    43.54, 43.61, 43.79, 43.80, 44.09, 44.16, 44.35, 44.44, 44.62,
                               44.89,    44.91, 45.11, 45.38, 45.80, 45.95, 46.51, 46.71, 46.74, 47.13],
            "top30_is_true":  [1] + [0]*29,
        },
        {
            "tok_idx": 4, "true_token": " the",
            "true_loss": 5.40e-11, "rank": 1,
            "min_rand": 32.10, "median_rand": 64.77, "sample_count": 256,
            "top30_losses":   [5.40e-11, 32.10, 33.55, 36.03, 36.14, 36.18, 36.78, 37.95, 38.02, 38.97,
                               39.79,    39.88, 40.46, 40.81, 41.21, 41.46, 42.03, 42.66, 42.74, 42.87,
                               43.31,    43.39, 43.39, 43.42, 43.59, 43.79, 44.21, 44.41, 44.78, 44.88],
            "top30_is_true":  [1] + [0]*29,
        },
        {
            "tok_idx": 5, "true_token": " mat",
            "true_loss": 3.71e-11, "rank": 1,
            "min_rand": 21.80, "median_rand": 65.62, "sample_count": 256,
            "top30_losses":   [3.71e-11, 21.80, 22.64, 23.58, 24.54, 24.84, 25.00, 25.25, 26.37, 27.29,
                               29.33,    30.71, 31.82, 32.13, 32.32, 32.99, 33.25, 34.31, 35.08, 35.50,
                               36.58,    37.24, 37.34, 37.53, 37.81, 38.04, 38.79, 39.04, 39.08, 40.25],
            "top30_is_true":  [1] + [0]*29,
        },
    ]

    # ── Pre-computed recovery results ─────────────────────────────────────────────
    # Prompt: "Hello world how", layer 12, full vocab (50,257 tokens), CPU.
    _RECOVERY_PREBAKED = [
        {"pos": 0, "true_word": "Hello",  "recovered_word": "Hello",  "min_loss": 2.50e-08, "correct": True},
        {"pos": 1, "true_word": " world", "recovered_word": " world", "min_loss": 7.74e-11, "correct": True},
        {"pos": 2, "true_word": " how",   "recovered_word": " how",   "min_loss": 1.12e-10, "correct": True},
    ]


    # ── Helper: project a hidden vector into the pre-fitted PCA space ─────────────
    def pca_project(hidden: np.ndarray) -> np.ndarray:
        """Project a (768,) hidden-state vector into 2-D using the stored PCA.

        This lets the notebook add a custom sentence to the PCA scatter without
        re-fitting the decomposition or importing scikit-learn at runtime.
        """
        return (hidden - _PCA_MEAN) @ _PCA_COMPONENTS.T

    def _pca_project(hidden):
        return (hidden - _PCA_MEAN) @ _PCA_COMPONENTS.T

    T = SimpleNamespace(
        TRUE_COLOR=_TRUE_COLOR, DIST_COLOR=_DIST_COLOR, WRONG_COLOR=_WRONG_COLOR,
        PCA_COLORS=_PCA_COLORS, RCPARAMS=_RCPARAMS,
        BASE_SENTENCES=_BASE_SENTENCES, XY_BASE=_XY_BASE,
        PCA_EXPLAINED_VARIANCE_RATIO=_PCA_EXPLAINED_VARIANCE_RATIO,
        MIN_PAIRWISE_DIST=_MIN_PAIRWISE_DIST,
        PCA_COMPONENTS=_PCA_COMPONENTS, PCA_MEAN=_PCA_MEAN,
        LANDSCAPE_PROMPT=_LANDSCAPE_PROMPT, LANDSCAPE_LAYER=_LANDSCAPE_LAYER,
        LANDSCAPE_RESULTS=_LANDSCAPE_RESULTS,
        RECOVERY_PREBAKED=_RECOVERY_PREBAKED,
        pca_project=_pca_project,
    )
    return T, mo, np, plt


@app.cell(hide_code=True)
def _palette(T):
    TRUE_COLOR  = T.TRUE_COLOR
    DIST_COLOR  = T.DIST_COLOR
    WRONG_COLOR = T.WRONG_COLOR
    PCA_COLORS  = T.PCA_COLORS
    return DIST_COLOR, PCA_COLORS, TRUE_COLOR, WRONG_COLOR


@app.cell(hide_code=True)
def _rcparams(T, plt):
    plt.rcParams.update(T.RCPARAMS)
    return


# ── Model loading (auto-runs; pre-computed cells don't depend on this) ─────────

@app.cell(hide_code=True)
def _model_load(mo):
    import torch
    import torch.nn as nn
    from transformers import GPT2Model, GPT2Tokenizer
    from transformers.cache_utils import DynamicCache

    with mo.status.spinner(
        title="Loading GPT-2 (~2–4 min on first visit in WASM, fast locally)…"
    ):
        tokenizer = GPT2Tokenizer.from_pretrained("gpt2")
        model     = GPT2Model.from_pretrained("gpt2")
        # Remove final layer norm: makes loss landscapes numerically cleaner.
        # This is a notebook presentation choice, not a theorem requirement.
        model.ln_f = nn.Identity()
        model.eval()
        device = torch.device("cpu")
        model  = model.to(device)

    return DynamicCache, device, model, nn, tokenizer, torch


@app.cell(hide_code=True)
def _model_utils(DynamicCache, torch):
    """Pure-utility functions shared across §2, §3, §5."""

    def expand_past_key_values(past_key_values, batch_size):
        if past_key_values is None:
            return None
        if hasattr(past_key_values, "layers"):
            expanded = DynamicCache()
            for li, layer in enumerate(past_key_values.layers):
                expanded.update(
                    layer.keys.expand(batch_size, -1, -1, -1).contiguous(),
                    layer.values.expand(batch_size, -1, -1, -1).contiguous(),
                    li,
                )
            return expanded
        return tuple(
            (k.expand(batch_size, -1, -1, -1).contiguous(),
             v.expand(batch_size, -1, -1, -1).contiguous())
            for k, v in past_key_values
        )

    def sample_without_true(vocab_size, sample_size, true_id, device):
        sample_size = max(0, min(sample_size, vocab_size - 1))
        if sample_size == 0:
            return torch.empty((0,), dtype=torch.long, device=device)
        draw = torch.randperm(vocab_size - 1, device=device)[:sample_size]
        return draw + (draw >= true_id).long()

    def add_noise(tensor, radius, seed):
        if radius <= 0.0:
            return tensor.clone()
        g = torch.Generator()
        g.manual_seed(seed)
        n = torch.randn(tensor.shape, generator=g)
        return tensor + radius * n / (n.norm() + 1e-12)

    def quantize(tensor, bits):
        if bits <= 0:
            return tensor.clone()
        mx = tensor.abs().max()
        if mx == 0:
            return tensor.clone()
        levels = (2 ** bits) - 1
        scaled = (tensor / mx + 1.0) / 2.0
        return ((scaled * levels).round() / levels * 2.0 - 1.0) * mx

    return add_noise, expand_past_key_values, quantize, sample_without_true


# ── Title & intro ──────────────────────────────────────────────────────────────

@app.cell(hide_code=True)
def _title(mo):
    mo.md(
        "# Language Models Are Injective, and Hence Invertible\n\n"
        "**Four stops:**\n"
        "1. The geometry of distinct prompts in hidden-state space, made visible on real GPT-2.\n"
        "2. The one-step loss landscape that singles out the true token at every position.\n"
        "3. Exact prompt recovery by exhaustive search over the full 50,257-token vocabulary.\n"
        "4. Where the recovery breaks, and what Theorem 3.2 promises about the half-margin bound.\n"
    )
    return


@app.cell(hide_code=True)
def _intro(mo):
    mo.md(
        r"""
The standard story about a language model goes as follows. A decoder-only transformer
maps an input sequence of tokens through many layers of attention and feed-forward
blocks, and at the top of the stack one reads off a probability over the next token.
The intermediate hidden states are usually treated as instrumental: useful for
prediction, not necessarily meaningful in themselves. The puzzle is that several
recent results suggest the opposite. Nikolaou et al.
([arXiv:2510.15511](https://arxiv.org/abs/2510.15511), ICLR 2026) prove that the
map from input sequences to hidden states is almost surely **injective**, meaning
two distinct prompts of the same length never collide at the same internal
representation, and that this property is preserved through training. Writing
$h_t(\pi)$ for the hidden state of a transformer at layer $t$ on prompt $\pi$, the
claim is that

$$
\pi \neq \pi' \quad\Longrightarrow\quad h_t(\pi) \neq h_t(\pi'),
$$

with probability one over a continuous initialization, and that gradient training
preserves the property up to a measure-zero exceptional set.

The practical consequence is sharper than the statement makes it sound. If the
map is injective then it is, in principle, invertible: anyone with access to a
model's hidden state can reconstruct the prompt that produced it. The paper
offers an algorithm, **SipIt**, that does this in linear time per token by exhaustive
search over the vocabulary. Theorem 3.2 then quantifies how much the hidden
states can be perturbed before recovery starts to fail, framed in terms of a
local separation margin that the geometry already supplies.

The remainder of this notebook works through the claim on the smallest model
that exhibits all of the relevant behavior: GPT-2 small, 124 M parameters, run
on CPU. The setting is rich enough to demonstrate every piece of the argument
the paper develops, and small enough that the inversion can be carried out live
in a browser tab.
        """.strip()
    )
    return


# ── § 1  PCA Sanity Check ──────────────────────────────────────────────────────

@app.cell(hide_code=True)
def _s1_header(mo):
    mo.md(
        "## First Steps: Distinct Prompts, Distinct Hidden States\n\n"
        "A natural starting point is to ask whether the injectivity claim is visible in "
        "the geometry of GPT-2 itself, before any inversion algorithm is brought to bear. "
        "Theorem 2.2 of the paper proves almost-sure injectivity at initialization, and "
        "Theorem 2.3 shows that gradient training preserves the property, but neither "
        "guarantee says anything about how *separated* distinct prompts actually are in "
        "practice. Two sequences could be technically distinct in hidden-state space and "
        "still sit on top of each other for all practical purposes, in which case "
        "recovery would be hopeless even though the map is formally one-to-one.\n\n"
        "The figure below takes the layer-12 hidden state of GPT-2 small for eight short "
        "sentences, mean-pools across positions to get a single 768-dimensional vector "
        "per sentence, and projects the result into two dimensions with PCA. The "
        "decomposition is fitted offline so the figure renders the moment the page loads. "
        "Type a sentence into the input below and the network will encode it once GPT-2 "
        "has finished initializing, projecting the result into the same pre-fitted PCA "
        "space so that it lands in the existing scatter as a black star."
    )
    return


@app.cell(hide_code=True)
def _s1_sentence_ui(mo):
    sentence_input = mo.ui.text(
        value="",
        placeholder="Type a sentence and press Enter…",
        label="Add your own sentence to the plot",
        full_width=True,
    )
    sentence_input
    return (sentence_input,)


@app.cell(hide_code=True)
def _s1_custom_vec(T, device, model, np, sentence_input, tokenizer, torch):
    """Project a custom sentence into the pre-fitted PCA space (requires GPT-2)."""
    custom = (sentence_input.value or "").strip()
    xy_custom = None
    if custom:
        with torch.no_grad():
            _ids = tokenizer.encode(custom, return_tensors="pt").to(device)
            _out = model(_ids, output_hidden_states=True)
            _h   = _out.hidden_states[-1].squeeze(0).mean(0).cpu().numpy()
        xy_custom = T.pca_project(_h)
    return custom, xy_custom


@app.cell(hide_code=True)
def _s1_plot(PCA_COLORS, T, custom, mo, np, plt, xy_custom):
    def _draw():
        fig, ax = plt.subplots(figsize=(9, 4.5), constrained_layout=True)

        for i, (pt, label) in enumerate(zip(T.XY_BASE, T.BASE_SENTENCES)):
            color = PCA_COLORS[i % len(PCA_COLORS)]
            ax.scatter(*pt, s=160, color=color, zorder=5)
            ax.annotate(
                f'"{label}"', pt,
                xytext=(7, 5), textcoords="offset points",
                fontsize=8.5, color=color,
            )

        if xy_custom is not None:
            ax.scatter(*xy_custom, s=280, color="#111", marker="*", zorder=10,
                       label="★  Your sentence")
            ax.annotate(
                f'"{custom}"', xy_custom,
                xytext=(7, 5), textcoords="offset points",
                fontsize=8.5, color="#111", fontweight="bold",
            )
            ax.legend(loc="lower right", fontsize=8.5, framealpha=0.8)

        ev = T.PCA_EXPLAINED_VARIANCE_RATIO
        ax.set_title(
            f"GPT-2 layer-12 hidden states — PCA projection (mean-pooled)\n"
            f"Minimum pairwise L2 distance across {len(T.BASE_SENTENCES)*(len(T.BASE_SENTENCES)-1)//2}"
            f" pairs: {T.MIN_PAIRWISE_DIST:.1f}",
            fontsize=10.5,
        )
        ax.set_xlabel(f"PC1 ({ev[0]:.0%} variance)")
        ax.set_ylabel(f"PC2 ({ev[1]:.0%} variance)")
        ax.grid(True, alpha=0.2)
        return fig

    _note = (
        f" The black star marks your sentence, **`\"{custom}\"`**, projected into the same space."
        if custom else
        " Type a sentence above to drop it onto the same plot as a black star, once GPT-2 has loaded."
    )
    mo.vstack([
        mo.center(_draw()),
        mo.md(
            f"Each of the {len(T.BASE_SENTENCES)} sentences lands at a distinct point in "
            f"the projection, with a minimum pairwise L2 distance of "
            f"{T.MIN_PAIRWISE_DIST:.1f} units across the {len(T.BASE_SENTENCES)*(len(T.BASE_SENTENCES)-1)//2} "
            f"pairs. The two cat sentences, which differ only in articles, sit close "
            f"together but are still cleanly separated; the literary fragment from "
            f'*Hamlet* lands far from the rest, as one would expect.{_note} The figure is '
            f"consistent with injectivity but not a proof of it: a 2D projection can "
            f"only ever rule out collisions in two coordinates, never confirm their "
            f"absence in 768. The next section turns to a sharper diagnostic that "
            f"actually probes the high-dimensional structure."
        ),
    ])
    return


# ── § 2  One-Step Loss Landscapes ─────────────────────────────────────────────

@app.cell(hide_code=True)
def _s2_header(mo):
    mo.md(
        r"""
## The One-Step Loss Landscape

The injectivity proof of the paper rests on a one-step argument. Fix a prefix
$\pi$ of tokens and ask, for every candidate next token $v$ in the vocabulary,
what hidden state the model produces for the extended sequence $\pi \oplus v$.
Theorem 2.2 says that the resulting map $v \mapsto h_t(\pi \oplus v)$ is
injective with probability one, which means exactly one candidate produces
the hidden state actually observed. Defining a per-candidate loss as the
squared distance between its hidden state and the observed one,

$$
\mathcal{L}(v) \;=\; \big\| h_t(\pi \oplus v) - h_t^{\mathrm{obs}} \big\|^2,
$$

the true token sits at zero and every other candidate sits strictly above it.
The picture is a flat landscape with a single sharp well at the right token.

The figure below tests this directly on GPT-2. For each of the six tokens in
the prompt *"The cat sat on the mat"* the notebook samples 256 random
distractor tokens, runs each through the model with the appropriate prefix,
and plots the top-30 lowest-loss candidates on a log scale. The leftmost bar
in green is always the true token, and the right tail in blue collects the
best-of-256 distractors. The result is pre-computed at layer 12 and renders
instantly. The live controls below let you re-run the same procedure on a
prompt of your choosing once GPT-2 has loaded.
        """.strip()
    )
    return


@app.cell(hide_code=True)
def _s2_prebaked_plot(DIST_COLOR, TRUE_COLOR, T, mo, np, plt):
    """Show the pre-computed loss landscape — no model needed."""
    def _draw():
        results = T.LANDSCAPE_RESULTS
        ncols = len(results)
        fig, axes = plt.subplots(1, ncols, figsize=(3.0 * ncols, 3.8),
                                 constrained_layout=True)
        if ncols == 1:
            axes = [axes]
        for res, ax in zip(results, axes):
            colors = [TRUE_COLOR if is_t else DIST_COLOR
                      for is_t in res["top30_is_true"]]
            vals = np.array(res["top30_losses"])
            plotted = np.where(vals <= 0, 1e-15, vals)
            ax.bar(range(len(vals)), plotted, color=colors, alpha=0.85, width=0.8)
            ax.set_yscale("log")
            ax.set_title(
                f"Pos {res['tok_idx']} → `{res['true_token']}`\n"
                f"True: {res['true_loss']:.1e}   Rank: {res['rank']}",
                fontsize=9,
            )
            ax.set_xlabel("Top-30 by loss")
            ax.set_ylabel("MSE loss (log)")
            ax.set_xticks([])
        return fig

    _ratios = [r["median_rand"] / max(r["true_loss"], 1e-15)
               for r in T.LANDSCAPE_RESULTS]
    _labels = [r["true_token"] for r in T.LANDSCAPE_RESULTS]
    def _draw_ratios():
        fig, ax = plt.subplots(figsize=(max(4, len(_ratios) * 1.2), 3.2),
                               constrained_layout=True)
        bars = ax.bar(_labels, _ratios, color=TRUE_COLOR, alpha=0.85, zorder=3)
        ax.set_yscale("log")
        ax.set_ylim(top=max(_ratios) * 20)
        ax.set_xlabel("Token")
        ax.set_ylabel("Margin ratio (log)")
        ax.set_title(
            "Sampled margin ratio: median distractor ÷ true-token loss\n"
            "Higher = true token more isolated",
            fontsize=9.5,
        )
        ax.grid(True, alpha=0.25, axis="y", zorder=0)
        for bar, val in zip(bars, _ratios):
            ax.text(bar.get_x() + bar.get_width() / 2, val * 2.5,
                    f"{val:.0e}", ha="center", va="bottom", fontsize=7.5)
        return fig

    mo.vstack([
        mo.center(_draw()),
        mo.md(
            f"The six panels record the loss landscape at each successive position of "
            f"*\"{T.LANDSCAPE_PROMPT}\"*. The first position has no preceding context, "
            f"and the true token *The* sits at exactly zero loss while the nearest "
            f"distractor lands at roughly 1.5; the gap is real but narrow. Once a "
            f"prefix exists, the picture sharpens dramatically. From position one "
            f"onward the true loss collapses to numerical noise (on the order of "
            f"10⁻¹¹) while distractors sit four to eleven orders of magnitude higher. "
            f"The right column of every panel records the **rank** of the true token "
            f"among the 256 candidates, which is **#1** at every position. The figure "
            f"below summarizes the same data as a margin ratio: median distractor loss "
            f"divided by true-token loss, position by position. The values span eleven "
            f"orders of magnitude on the log scale, which is what permits exhaustive "
            f"search to recover the prompt with no ambiguity at all."
        ),
        mo.center(_draw_ratios()),
    ])
    return


@app.cell(hide_code=True)
def _s2_controls(mo):
    prompt_input = mo.ui.text(
        value="The cat sat on the mat", label="Prompt", full_width=True,
    )
    layer_slider = mo.ui.slider(
        start=1, stop=12, step=1, value=12,
        label="Target layer", show_value=True, full_width=True,
    )
    n_sample_slider = mo.ui.slider(
        start=128, stop=2048, step=128, value=512,
        label="Distractor tokens per position", show_value=True, full_width=True,
    )
    run_btn = mo.ui.run_button(label="Run live landscape")
    mo.vstack([
        mo.md(
            "The same diagnostic on a prompt of your choosing. The number of distractors "
            "controls how thoroughly the landscape is sampled, and the layer slider lets "
            "you watch how the gap widens as one moves up the stack."
        ),
        prompt_input,
        mo.hstack([layer_slider, n_sample_slider], widths="equal"),
        run_btn,
    ], gap=0.5)
    return layer_slider, n_sample_slider, prompt_input, run_btn


@app.cell(hide_code=True)
def _s2_token_preview(mo, prompt_input, tokenizer):
    _ids = tokenizer.encode(prompt_input.value or "")
    _prev = "  ".join(f"`{tokenizer.decode([t])}`" for t in _ids) if _ids else "_empty_"
    mo.md(f"**Tokens ({len(_ids)}):** {_prev}")
    return


@app.cell(hide_code=True)
def _s2_compute(
    device, expand_past_key_values, layer_slider,
    model, mo, n_sample_slider, np, prompt_input,
    run_btn, sample_without_true, tokenizer, torch,
):
    landscape_prompt = None
    landscape_results = None

    if run_btn.value:
        _prompt     = (prompt_input.value or "").strip()
        _layer_idx  = int(layer_slider.value)
        _n_sample   = int(n_sample_slider.value)
        _true_ids   = tokenizer.encode(_prompt)
        _vocab_size = model.wte.weight.shape[0]
        _batch_size = 512

        if not _true_ids:
            landscape_prompt  = _prompt
            landscape_results = []
        else:
            with torch.no_grad():
                _tgt_all = model(
                    torch.tensor(_true_ids).unsqueeze(0), output_hidden_states=True
                ).hidden_states[_layer_idx][0].detach()

            _results = []
            with mo.status.progress_bar(
                total=len(_true_ids), title="Computing loss landscapes…"
            ) as _bar:
                for _ti, _true_id in enumerate(_true_ids):
                    _tgt_h     = _tgt_all[_ti]
                    _distractors = sample_without_true(_vocab_size, _n_sample, _true_id, device)
                    _cands     = torch.cat([torch.tensor([_true_id], device=device), _distractors])
                    _pcache    = None
                    if _ti > 0:
                        with torch.no_grad():
                            _pcache = model(
                                torch.tensor(_true_ids[:_ti], device=device).unsqueeze(0),
                                use_cache=True,
                            ).past_key_values

                    _losses = []
                    for _s in range(0, len(_cands), _batch_size):
                        _e  = min(_s + _batch_size, len(_cands))
                        _bc = _cands[_s:_e].unsqueeze(1)
                        with torch.no_grad():
                            _kw = dict(output_hidden_states=True)
                            if _pcache is not None:
                                _kw["past_key_values"] = expand_past_key_values(_pcache, _e - _s)
                            _h = model(_bc, **_kw).hidden_states[_layer_idx][:, -1, :]
                        _losses.append(((_h - _tgt_h) ** 2).mean(1).cpu())

                    _all_l  = torch.cat(_losses).numpy()
                    _tl     = float(_all_l[0])
                    _rl     = _all_l[1:]
                    _sorted = np.argsort(_all_l)[:30]
                    _results.append({
                        "tok_idx": _ti,
                        "true_token": tokenizer.decode([int(_true_id)]),
                        "true_loss": _tl,
                        "rank": int((_all_l < _tl).sum() + 1),
                        "min_rand": float(_rl.min()) if len(_rl) else float("nan"),
                        "median_rand": float(np.median(_rl)) if len(_rl) else float("nan"),
                        "sample_count": len(_rl),
                        "top30_losses": _all_l[_sorted].tolist(),
                        "top30_is_true": [int(i == 0) for i in _sorted],
                    })
                    _bar.update()

            landscape_prompt  = _prompt
            landscape_results = _results

    return landscape_prompt, landscape_results


@app.cell(hide_code=True)
def _s2_live_plot(DIST_COLOR, TRUE_COLOR, landscape_prompt, landscape_results, mo, np, plt):
    if landscape_results is None:
        _display = mo.md(
            "_Click **Run live landscape** above to compute the same diagnostic for "
            "a custom prompt._"
        )
    elif len(landscape_results) == 0:
        _display = mo.md("_Prompt has zero tokens. Try a different input._")
    else:
        _ncols = min(len(landscape_results), 6)
        _fig, _axes = plt.subplots(
            1, _ncols, figsize=(3.0 * _ncols, 3.8), constrained_layout=True
        )
        if _ncols == 1:
            _axes = [_axes]
        for _res, _ax in zip(landscape_results[:_ncols], _axes):
            _colors  = [TRUE_COLOR if _it else DIST_COLOR for _it in _res["top30_is_true"]]
            _vals    = np.array(_res["top30_losses"])
            _plotted = np.where(_vals <= 0, 1e-15, _vals)
            _ax.bar(range(len(_vals)), _plotted, color=_colors, alpha=0.85, width=0.8)
            _ax.set_yscale("log")
            _ax.set_title(
                f"Pos {_res['tok_idx']} → `{_res['true_token']}`\n"
                f"True: {_res['true_loss']:.1e}   Rank: {_res['rank']}",
                fontsize=9,
            )
            _ax.set_xlabel("Top-30 by loss")
            _ax.set_ylabel("MSE loss (log)")
            _ax.set_xticks([])

        _ratios = [r["median_rand"] / max(r["true_loss"], 1e-15) for r in landscape_results]
        _labels = [r["true_token"] for r in landscape_results]
        _fig2, _ax2 = plt.subplots(
            figsize=(max(4, len(_ratios) * 1.2), 3.2), constrained_layout=True
        )
        _bars = _ax2.bar(_labels, _ratios, color=TRUE_COLOR, alpha=0.85, zorder=3)
        _ax2.set_yscale("log")
        _ax2.set_ylim(top=max(_ratios) * 20)
        _ax2.set_xlabel("Token"); _ax2.set_ylabel("Margin ratio (log)")
        _ax2.set_title("Sampled margin ratio: median distractor ÷ true-token loss", fontsize=9.5)
        _ax2.grid(True, alpha=0.25, axis="y", zorder=0)
        for _bar, _val in zip(_bars, _ratios):
            _ax2.text(_bar.get_x() + _bar.get_width() / 2, _val * 2.5,
                      f"{_val:.0e}", ha="center", va="bottom", fontsize=7.5)

        _all_r1  = all(r["rank"] == 1 for r in landscape_results)
        _rows    = "\n".join(
            f"| `{r['true_token']}` | {r['true_loss']:.2e} | "
            f"{r['min_rand']:.2e} | {r['median_rand']:.2e} | **{r['rank']}** |"
            for r in landscape_results
        )
        _table = (
            "| Token | True loss | Min dist. | Median dist. | Rank |\n"
            "|---|---:|---:|---:|---:|\n" + _rows
        )
        _summary = (
            f"The true token ranked **#1** at every position of *\"{landscape_prompt}\"*, "
            f"reproducing the pattern from the pre-computed example."
            if _all_r1 else
            f"At least one position of *\"{landscape_prompt}\"* missed. The most common "
            f"cause is too few distractors at a near-collision; try increasing the "
            f"sample count, or shortening the prompt."
        )
        _display = mo.vstack([
            mo.center(_fig),
            mo.center(_fig2),
            mo.md(_summary),
            mo.md(_table),
        ])
    _display
    return


# ── § 3  Exact Prompt Recovery ─────────────────────────────────────────────────

@app.cell(hide_code=True)
def _s3_header(mo):
    mo.md(
        r"""
## Exact Prompt Recovery

The previous section establishes that the true token is the unique near-zero
minimizer of the one-step loss when 256 random distractors are thrown against
it. The injectivity claim of the paper is stronger: it says the true token is
the unique near-zero minimizer when **every** token in the vocabulary is
considered. SipIt, the recovery algorithm of Section 3, takes this literally.
At each position $i$ it sweeps the entire vocabulary $\mathcal{V}$, runs each
candidate through the model with the recovered prefix $\hat\pi_{<i}$, and
picks the token whose hidden state matches the observed one most closely:

$$
\hat v_i \;=\; \arg\min_{v \in \mathcal{V}}\, \big\| h_t(\hat\pi_{<i} \oplus v) - h_t^{\mathrm{obs}}[i] \big\|^2.
$$

Because the loss landscape has a single sharp well at the right token, the
procedure recovers the original prompt exactly, in time linear in the prompt
length and the vocabulary size.

The result for *"Hello world how"* is shown below. The recovery was carried
out at layer 12 over the full vocabulary, so each value is a true minimum over
50,257 candidates rather than over a sample. The minimum MSE at each position
sits in the $10^{-8}$ to $10^{-11}$ range, which is several orders of magnitude
lower than the next-best distractor and well within the noise floor of the
floating-point arithmetic. Recovery is exact at every position. Live controls
follow for verifying the result on a short prompt of your own choosing.
        """.strip()
    )
    return


@app.cell(hide_code=True)
def _s3_prebaked(T, mo):
    _r = T.RECOVERY_PREBAKED
    mo.vstack([
        mo.hstack(
            [
                mo.stat(
                    value=row["true_word"].strip() or row["true_word"],
                    label=f"Position {row['pos']}",
                    caption=f"min MSE {row['min_loss']:.2e}",
                    bordered=True,
                )
                for row in _r
            ],
            justify="start",
        ),
        mo.md(
            "Each box reports one position of the recovered prompt. The true token "
            "appears as the value, the position index as the label, and the minimum "
            "MSE achieved over the full vocabulary as the caption. The three boxes "
            "concatenate to *\"Hello world how\"*, the original prompt, and every "
            "minimum loss is at least eight orders of magnitude below the next-best "
            "distractor. The loss landscape has a single sharp minimum at the right "
            "token and exhaustive search lands on it without ambiguity."
        ),
    ])
    return


@app.cell(hide_code=True)
def _s3_controls(mo):
    recover_input = mo.ui.text(
        value="Hello world how", label="Prompt to invert", full_width=True,
    )
    recover_layer = mo.ui.slider(
        start=1, stop=12, step=1, value=12,
        label="Recovery layer", show_value=True, full_width=True,
    )
    recover_batch = mo.ui.slider(
        steps=[128, 256, 512, 1024], value=1024,
        label="Candidate batch size", show_value=True, full_width=True,
    )
    recover_btn = mo.ui.run_button(label="Recover exactly (full vocabulary)")
    mo.vstack([
        mo.md(
            "Live recovery on a prompt of your choosing. The full-vocabulary sweep "
            "evaluates roughly 50,257 candidates per position, which takes on the "
            "order of 20–50 seconds per token in WASM, so keep prompts to one to "
            "three tokens."
        ),
        recover_input,
        mo.hstack([recover_layer, recover_batch], widths="equal"),
        recover_btn,
    ], gap=0.5)
    return recover_batch, recover_btn, recover_input, recover_layer


@app.cell(hide_code=True)
def _s3_token_preview(mo, recover_input, tokenizer):
    _ids = tokenizer.encode(recover_input.value or "")
    _prev = "  ".join(f"`{tokenizer.decode([t])}`" for t in _ids) if _ids else "_empty_"
    mo.md(f"**Tokens ({len(_ids)}):** {_prev}")
    return


@app.cell(hide_code=True)
def _s3_compute(
    device, expand_past_key_values, model, mo,
    recover_batch, recover_btn, recover_input, recover_layer,
    tokenizer, torch,
):
    recovery_prompt  = None
    recovery_results = None
    recovery_config  = None

    if recover_btn.value:
        _prompt     = (recover_input.value or "").strip()
        _layer_idx  = int(recover_layer.value)
        _batch_size = int(recover_batch.value)
        _config     = {"layer": _layer_idx, "batch_size": _batch_size, "status": "ok"}
        _true_ids   = tokenizer.encode(_prompt)
        _vocab_size = model.wte.weight.shape[0]

        if not _true_ids:
            _config["status"] = "empty"
        elif len(_true_ids) > 6:
            _config["status"] = "too_long"
        else:
            _targets = []
            with torch.no_grad():
                for _pos in range(len(_true_ids)):
                    _out = model(
                        torch.tensor(_true_ids[:_pos + 1], device=device).unsqueeze(0),
                        output_hidden_states=True,
                    )
                    _targets.append(_out.hidden_states[_layer_idx][0, -1, :].detach())

            _recovered = []
            _results   = []
            with mo.status.progress_bar(
                total=len(_true_ids), title="Recovering tokens…"
            ) as _bar:
                for _pos in range(len(_true_ids)):
                    _tgt_h   = _targets[_pos]
                    _pcache  = None
                    if _recovered:
                        with torch.no_grad():
                            _pcache = model(
                                torch.tensor(_recovered, device=device).unsqueeze(0),
                                use_cache=True,
                            ).past_key_values

                    _best_tok  = 0
                    _best_loss = float("inf")
                    for _s in range(0, _vocab_size, _batch_size):
                        _e  = min(_s + _batch_size, _vocab_size)
                        _bc = torch.arange(_s, _e, device=device).unsqueeze(1)
                        with torch.no_grad():
                            _kw = dict(output_hidden_states=True)
                            if _pcache is not None:
                                _kw["past_key_values"] = expand_past_key_values(_pcache, _e - _s)
                            _h = model(_bc, **_kw).hidden_states[_layer_idx][:, -1, :]
                        _l   = ((_h - _tgt_h) ** 2).mean(1)
                        _bi  = int(_l.argmin())
                        if float(_l[_bi]) < _best_loss:
                            _best_loss = float(_l[_bi])
                            _best_tok  = _s + _bi

                    _recovered.append(_best_tok)
                    _results.append({
                        "pos": _pos,
                        "true_id": _true_ids[_pos],
                        "recovered_id": _best_tok,
                        "true_word": tokenizer.decode([_true_ids[_pos]]),
                        "recovered_word": tokenizer.decode([_best_tok]),
                        "min_loss": _best_loss,
                        "correct": _best_tok == _true_ids[_pos],
                    })
                    _bar.update()

            recovery_prompt  = _prompt
            recovery_results = _results
            recovery_config  = _config

        if _config["status"] != "ok":
            recovery_prompt  = _prompt
            recovery_results = []
            recovery_config  = _config

    return recovery_config, recovery_prompt, recovery_results


@app.cell(hide_code=True)
def _s3_live_plot(mo, recovery_config, recovery_prompt, recovery_results):
    if recovery_results is None:
        _display = mo.md(
            "_Click **Recover exactly** above to run the live full-vocabulary search "
            "on your own prompt._"
        )
    elif recovery_config["status"] == "empty":
        _display = mo.md("_Zero tokens. Try a different prompt._")
    elif recovery_config["status"] == "too_long":
        _display = mo.md(
            "_Capped at six tokens to keep the WASM run-time bearable. Shorten the prompt._"
        )
    else:
        _all_ok  = all(r["correct"] for r in recovery_results)
        _rec_str = "".join(r["recovered_word"] for r in recovery_results)
        _rows    = "\n".join(
            f"| {r['pos']} | `{r['true_word']}` | `{r['recovered_word']}` "
            f"| {r['min_loss']:.2e} | {'✓' if r['correct'] else '✗'} |"
            for r in recovery_results
        )
        _table = (
            "| Pos | True token | Recovered | Min loss | Match |\n"
            "|---|---|---|---:|:---:|\n" + _rows
        )
        _summary = (
            f"Recovery of *\"{recovery_prompt}\"* at layer {recovery_config['layer']} "
            f"returned **\"{_rec_str}\"**, an exact match at every position."
            if _all_ok else
            f"Recovery of *\"{recovery_prompt}\"* at layer {recovery_config['layer']} "
            f"returned **\"{_rec_str}\"**, with at least one position diverging from "
            f"the truth. Reducing the recovery layer or rerunning with a smaller "
            f"batch size sometimes recovers the missed token."
        )
        _display = mo.vstack([
            mo.hstack(
                [mo.stat(
                    value=r["recovered_word"].strip() or r["recovered_word"],
                    label=f"Position {r['pos']}",
                    caption=f"min MSE {r['min_loss']:.2e}",
                    bordered=True,
                ) for r in recovery_results],
                justify="start", wrap=True,
            ),
            mo.md(_summary),
            mo.md(_table),
        ])
    _display
    return


# ── § 4  Honest Scope ─────────────────────────────────────────────────────────

@app.cell(hide_code=True)
def _s4_scope(mo):
    mo.vstack([
        mo.md(
            "## A Note on Scope\n\n"
            "The exact recovery of the previous section is striking, and it is worth "
            "being clear about exactly what it does and does not establish before "
            "pushing the argument further. The three accordion items below cover the "
            "three questions readers ask most often in this order: what each section "
            "actually proves, what threat model the recovery is operating under, and "
            "where the underlying theorems sit in the paper."
        ),
        mo.accordion({
            "1. What each section actually verifies":
                mo.md(
                    "The 2-D PCA scatter of § 1 is consistent with the injectivity "
                    "claim but does not prove it: a projection into two coordinates "
                    "can only ever rule out collisions in those two, never confirm "
                    "their absence in the remaining 766. The sampled loss landscapes "
                    "of § 2 are diagnostics rather than proofs, since they only verify "
                    "separation against 256 random distractors per position. The "
                    "exhaustive full-vocabulary search of § 3 is the only one of the "
                    "three that actually verifies injectivity in the setting the "
                    "paper analyzes; it leaves no candidate untested. § 5 then "
                    "stress-tests the recovery against the perturbation budget of "
                    "Theorem 3.2 and reproduces the half-margin transition directly."
                ),
            "2. The threat model is hidden-state leakage":
                mo.md(
                    "The recovery procedure assumes access to the hidden-state "
                    "sequence at a fixed layer, not just to the model's output text. "
                    "An adversary who only sees sampled tokens, logits, or any of "
                    "the other interfaces that production systems typically expose "
                    "is not in a position to run SipIt. The attack surface this "
                    "addresses is **activation leakage**: hidden states intercepted "
                    "in transit, dumped to disk, exposed by an introspection API, "
                    "or otherwise made visible to a party that should not have them. "
                    "Recovery from output text alone is a strictly harder problem "
                    "and out of scope here."
                ),
            "3. Where the theorems sit in the paper":
                mo.md(
                    "**Theorem 2.2** (almost-sure injectivity at initialization) is "
                    "the foundational result and follows from a measure-theoretic "
                    "argument over the continuous parameter space of a decoder-only "
                    "transformer. **Theorem 2.3** lifts this to the trained model by "
                    "showing that finite-step gradient descent preserves the property "
                    "up to a measure-zero exceptional set. **Theorem 3.2** is the "
                    "robustness statement and gives the half-margin bound that § 5 "
                    "tests directly. The constructive recovery algorithm SipIt is "
                    "stated as Algorithm 1 in Section 3 and runs in time linear in "
                    "the prompt length and the vocabulary size."
                ),
        }, multiple=True),
    ], gap=0.5)
    return


# ── § 5  Noise & Quantization Robustness ──────────────────────────────────────

@app.cell(hide_code=True)
def _s5_header(mo):
    mo.md(
        r"""
## When Recovery Breaks

Exact recovery on clean hidden states is the easy case. A more realistic
threat model assumes the attacker sees a *perturbed* version of the activations,
either because they were intercepted across a noisy channel or because the
model serialized them at reduced precision. Theorem 3.2 of the paper handles
this setting and gives a quantitative answer: recovery succeeds whenever the
per-position perturbation $\delta_i$ stays below **half the local separation
margin** between the true token's hidden state and the nearest distractor's,

$$
\|\delta_i\| \;<\; \tfrac{1}{2}\, \min_{v \neq v_i^\star}
  \big\| h_t(\pi_{<i} \oplus v) - h_t(\pi_{<i} \oplus v_i^\star) \big\|.
$$

The constant $\tfrac{1}{2}$ is sharp, in the sense that adversarial perturbations
exactly at the threshold can flip a recovery, and the bound is otherwise tight in
expectation.

The cell below tests the bound directly on GPT-2. The clean layer-12 hidden
states are computed for a short prompt, then perturbed by additive isotropic
Gaussian noise of a chosen radius, optionally quantized to a chosen bit-width,
and finally fed back into the same exhaustive-search procedure as in the
previous section. Starting at noise zero and quantization off reproduces the
clean recovery as a sanity check; turning either knob up forces the perturbation
across the half-margin threshold, at which point the recovery degrades. The
per-position perturbation norm is plotted in green when recovery succeeded
at that position and red when it failed.
        """.strip()
    )
    return


@app.cell(hide_code=True)
def _s5_controls(mo):
    robust_input = mo.ui.text(
        value="Hello world", label="Prompt to perturb", full_width=True,
    )
    robust_layer = mo.ui.slider(
        start=1, stop=12, step=1, value=12,
        label="Target layer", show_value=True, full_width=True,
    )
    robust_batch = mo.ui.slider(
        steps=[128, 256, 512, 1024], value=512,
        label="Candidate batch size", show_value=True, full_width=True,
    )
    robust_noise = mo.ui.slider(
        start=0.0, stop=5.0, step=0.25, value=0.0,
        label="Noise radius ‖δ‖", show_value=True, full_width=True,
    )
    robust_quant = mo.ui.slider(
        steps=[0, 4, 8], value=0,
        label="Quantization bits (0 = off)", show_value=True, full_width=True,
    )
    robust_seed = mo.ui.slider(
        start=0, stop=20, step=1, value=0,
        label="Noise seed", show_value=True, full_width=True,
    )
    robust_btn = mo.ui.run_button(label="Run perturbed recovery")
    mo.vstack([
        mo.md(
            "Sliders control prompt, target layer, and the two perturbations. Noise "
            "radius is the L2 norm of the additive Gaussian shift; quantization bits "
            "set the resolution of a uniform mid-tread quantizer applied after the "
            "noise. The seed reshuffles the noise without changing its norm."
        ),
        robust_input,
        mo.hstack([robust_layer, robust_batch], widths="equal"),
        mo.hstack([robust_noise, robust_quant], widths="equal"),
        robust_seed,
        robust_btn,
    ], gap=0.5)
    return (
        robust_batch, robust_btn, robust_input,
        robust_layer, robust_noise, robust_quant, robust_seed,
    )


@app.cell(hide_code=True)
def _s5_token_preview(mo, robust_input, tokenizer):
    _ids = tokenizer.encode(robust_input.value or "")
    _prev = "  ".join(f"`{tokenizer.decode([t])}`" for t in _ids) if _ids else "_empty_"
    mo.md(f"**Tokens ({len(_ids)}):** {_prev}")
    return


@app.cell(hide_code=True)
def _s5_compute(
    add_noise, device, expand_past_key_values, model, mo, quantize,
    robust_batch, robust_btn, robust_input, robust_layer,
    robust_noise, robust_quant, robust_seed,
    tokenizer, torch,
):
    robust_prompt  = None
    robust_results = None
    robust_config  = None

    if robust_btn.value:
        _prompt      = (robust_input.value or "").strip()
        _layer_idx   = int(robust_layer.value)
        _batch_size  = int(robust_batch.value)
        _noise_r     = float(robust_noise.value)
        _quant_bits  = int(robust_quant.value)
        _seed        = int(robust_seed.value)
        _config      = {
            "layer": _layer_idx, "batch_size": _batch_size,
            "noise_radius": _noise_r, "quant_bits": _quant_bits,
            "seed": _seed, "status": "ok",
        }
        _true_ids    = tokenizer.encode(_prompt)
        _vocab_size  = model.wte.weight.shape[0]

        if not _true_ids:
            _config["status"] = "empty"
        elif len(_true_ids) > 6:
            _config["status"] = "too_long"
        else:
            _clean = []
            with torch.no_grad():
                for _pos in range(len(_true_ids)):
                    _out = model(
                        torch.tensor(_true_ids[:_pos + 1], device=device).unsqueeze(0),
                        output_hidden_states=True,
                    )
                    _clean.append(_out.hidden_states[_layer_idx][0, -1, :].detach())

            _perturbed = []
            _pert_norms = []
            for _pos, _tgt in enumerate(_clean):
                _noisy = add_noise(_tgt, _noise_r, _seed * 1000 + _pos)
                _quant = quantize(_noisy, _quant_bits)
                _perturbed.append(_quant)
                _pert_norms.append(float((_quant - _tgt).norm()))

            _recovered = []
            _results   = []
            with mo.status.progress_bar(
                total=len(_true_ids), title="Recovering from perturbed hidden states…"
            ) as _bar:
                for _pos in range(len(_true_ids)):
                    _tgt_h  = _perturbed[_pos]
                    _pcache = None
                    if _recovered:
                        with torch.no_grad():
                            _pcache = model(
                                torch.tensor(_recovered, device=device).unsqueeze(0),
                                use_cache=True,
                            ).past_key_values

                    _best_tok  = 0
                    _best_loss = float("inf")
                    for _s in range(0, _vocab_size, _batch_size):
                        _e  = min(_s + _batch_size, _vocab_size)
                        _bc = torch.arange(_s, _e, device=device).unsqueeze(1)
                        with torch.no_grad():
                            _kw = dict(output_hidden_states=True)
                            if _pcache is not None:
                                _kw["past_key_values"] = expand_past_key_values(_pcache, _e - _s)
                            _h = model(_bc, **_kw).hidden_states[_layer_idx][:, -1, :]
                        _l  = ((_h - _tgt_h) ** 2).mean(1)
                        _bi = int(_l.argmin())
                        if float(_l[_bi]) < _best_loss:
                            _best_loss = float(_l[_bi])
                            _best_tok  = _s + _bi

                    _recovered.append(_best_tok)
                    _results.append({
                        "pos": _pos,
                        "true_word": tokenizer.decode([_true_ids[_pos]]),
                        "recovered_word": tokenizer.decode([_best_tok]),
                        "perturbation_norm": _pert_norms[_pos],
                        "min_loss": _best_loss,
                        "correct": _best_tok == _true_ids[_pos],
                    })
                    _bar.update()

            robust_prompt  = _prompt
            robust_results = _results
            robust_config  = _config

        if _config["status"] != "ok":
            robust_prompt  = _prompt
            robust_results = []
            robust_config  = _config

    return robust_config, robust_prompt, robust_results


@app.cell(hide_code=True)
def _s5_live_plot(TRUE_COLOR, WRONG_COLOR, mo, plt, robust_config, robust_prompt, robust_results):
    if robust_results is None:
        _display = mo.md(
            "_Click **Run perturbed recovery** above. Start at noise = 0 and "
            "quantization off to confirm the clean baseline, then turn either knob up._"
        )
    elif robust_config["status"] == "empty":
        _display = mo.md("_Zero tokens. Try a different prompt._")
    elif robust_config["status"] == "too_long":
        _display = mo.md("_Capped at six tokens. Shorten the prompt._")
    else:
        _all_ok  = all(r["correct"] for r in robust_results)
        _rec_str = "".join(r["recovered_word"] for r in robust_results)
        _nr      = robust_config["noise_radius"]
        _qb      = robust_config["quant_bits"]
        _noise_s = f"noise ‖δ‖ = {_nr:.2f}"
        _quant_s = f"{_qb}-bit quant" if _qb else "no quant"

        _fig, _ax = plt.subplots(
            figsize=(max(4.0, len(robust_results) * 1.5), 3.5),
            constrained_layout=True,
        )
        _labels = [repr(r["true_word"]).strip("'") for r in robust_results]
        _norms  = [r["perturbation_norm"] for r in robust_results]
        _bcolors = [TRUE_COLOR if r["correct"] else WRONG_COLOR for r in robust_results]
        _ax.bar(_labels, _norms, color=_bcolors, alpha=0.85)
        _ax.set_xlabel("Token")
        _ax.set_ylabel("Perturbation ‖δ‖")
        _ax.set_title(
            "Per-position perturbation norm\n(green = correct, red = wrong)",
            fontsize=9.5,
        )
        _ax.grid(True, alpha=0.2, axis="y")

        _rows = "\n".join(
            f"| {r['pos']} | `{r['true_word']}` | `{r['recovered_word']}` "
            f"| {r['perturbation_norm']:.4f} | {r['min_loss']:.2e} | "
            f"{'✓' if r['correct'] else '✗'} |"
            for r in robust_results
        )
        _table = (
            "| Pos | True | Recovered | ‖δ‖ | Min loss | Match |\n"
            "|---|---|---|---:|---:|:---:|\n" + _rows
        )
        _verdict = (
            f"With {_noise_s} and {_quant_s}, recovery of *\"{robust_prompt}\"* "
            f"**succeeded** at every position and returned **\"{_rec_str}\"**. "
            f"The perturbation stayed below the half-margin threshold at all six "
            f"positions, exactly as Theorem 3.2 predicts."
            if _all_ok else
            f"With {_noise_s} and {_quant_s}, recovery of *\"{robust_prompt}\"* "
            f"**failed** and returned **\"{_rec_str}\"**. The perturbation crossed "
            f"the half-margin threshold at the positions plotted in red, and the "
            f"exhaustive search latched onto a near-neighbor token instead. The "
            f"transition is sharp: small reductions in noise often restore exact "
            f"recovery, and small increases push more positions over the edge."
        )
        _display = mo.vstack([
            mo.center(_fig),
            mo.md(_verdict),
            mo.md(_table),
        ])
    _display
    return


# ── Takeaways ──────────────────────────────────────────────────────────────────

@app.cell(hide_code=True)
def _takeaways(mo):
    mo.md(
        r"""
## Onwards

A number of related observations have come together in the course of the
preceding sections, and it may help to summarize the picture they form before
turning to what remains open. The PCA scatter of the first section is the
weakest of the three diagnostics but the easiest to read: distinct prompts
land at distinct points in two dimensions, which is consistent with
injectivity in 768. The one-step loss landscape of the second section is
sharper and shows the true token sitting eight to eleven orders of magnitude
below the nearest sampled distractor, which is what makes exhaustive search
viable. The exhaustive search of the third section is the only verifier of
injectivity that is honest in the strict sense: at every position the entire
vocabulary of 50,257 tokens is checked, and the true token is the unique
minimum every time. The fifth section pushes the same procedure into a
noisier setting and reproduces the half-margin bound of Theorem 3.2 directly
on GPT-2: small perturbations of the hidden states leave recovery intact,
while perturbations exceeding the local separation margin flip individual
positions in a sharp transition.

The five sections then collapse into the following one-line summary, with
each row pointing at the part of the paper it verifies and the strength of
the evidence the notebook supplies:

| Section | What it shows | Paper reference | Verifier strength |
|---|---|---|---|
| § 1 | Distinct prompts land at distinct points in PCA(2) | Thm 2.2 / 2.3 | suggestive only |
| § 2 | True token is the unique near-zero minimizer vs 256 distractors | Lemma 3.1 | sampled |
| § 3 | True token is the unique minimizer over **all** 50,257 candidates | Algorithm 1 (SipIt) | exhaustive |
| § 5 | Recovery survives $\|\delta\| < \tfrac{1}{2}\,\mathrm{margin}$, breaks above | Thm 3.2 | empirical |

Several questions remain open. The notebook works exclusively with GPT-2 small,
which is the smallest model in the family that exhibits the relevant geometry,
but the paper's argument should hold for any decoder-only transformer with the
same architectural features and would be worth verifying on a contemporary model.
The recovery procedure was carried out at layer 12, the final hidden layer,
and the question of how the separation margin varies across depth has not been
addressed here. The threat model assumed throughout is hidden-state leakage
from a fixed layer; recovery from logits, sampled output text, or any of the
other interfaces production systems expose remains out of scope and is, on the
evidence available, a much harder problem.

**The picture in four lines, for the reader who has scrolled to the end:**

1. Distinct prompts produce distinct hidden states in GPT-2; the geometry is
   consistent with the almost-sure injectivity proof of Theorems 2.2 and 2.3.
2. The one-step loss landscape has a single sharp well at the true token, with
   distractors sitting orders of magnitude higher even at random.
3. Exhaustive search over all 50,257 GPT-2 tokens recovers the prompt **exactly**,
   with minimum MSE on the order of $10^{-10}$.
4. Recovery survives small perturbations of the hidden states and breaks at
   the half-margin threshold of Theorem 3.2, in a sharp transition rather than
   a gradual one.

Paper: [arXiv:2510.15511](https://arxiv.org/abs/2510.15511) · Nikolaou et al.,
*Language Models Are Injective and Hence Invertible*, ICLR 2026. Notebook for
the alphaXiv × marimo competition.
        """.strip()
    )
    return


if __name__ == "__main__":
    app.run()
